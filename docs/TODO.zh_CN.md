# TODO List（zh_CN）

最近整理日期：2026-05-15。

本文收录用户提出的后续待办，并按“低风险快修优先、影响工作流的大改后置、可并行的品牌/网络能力单独排期”的原则排序。它不是已经实现的功能证明；实施前必须重新核对当前代码和相关架构文档。

## 建议执行顺序

| 顺序 | 待办 | 优先级 | 规模 | 主要验收点 |
| --- | --- | --- | --- | --- |
| 1 | 滑条在手动改数字之后要跟着变 | 高 | 小 | 所有关联滑条 / 数字输入双向同步，手动输入后 UI、配置和后续请求值一致。 |
| 2 | 更新提示词以尽量避免安全拒绝 | 高 | 中 | 只改 prompt 存储文件，不把 prompt 硬编码进代码；明确任务是内容分析和角色信息提取，不请求生成违规内容；仍保持 JSON 输出约束。 |
| 3 | 评估提取模式选项位置 | 中 | 小 | 评估是否应把 PREVIEW / FULL 模式、服务商拒绝跳过选项、开始按钮和状态反馈移到洞察小溪下方；形成取舍结论和后续实施建议，再决定是否进入 UI 改造。 |
| 4 | 整理输出角色卡空间 | 中 | 中 | 输出页能清楚区分空状态、预览结果、正式编译结果和导出区域；角色卡内容有稳定可读的展示空间。 |
| 5 | 目标角色移动到角色卡页面，提取只提取素材信息，角色卡页面编译角色卡 | 高 | 大 | 提取阶段不再依赖目标角色；角色卡页面负责选择目标角色并从知识库编译角色卡；涉及数据流和 UI 边界，实施前需要单独计划。 |
| 6 | 内置 proxy 设置 | 中 | 中/大 | 设置页可配置代理；模型请求和必要网络请求统一走中间件读取代理；日志不泄露代理账号、密码或密钥。 |
| 7 | 软件 logo | 低 | 中 | 产出应用图标和必要资源尺寸；同步窗口图标、打包资源和文档展示入口。 |

## 实施注意

- UI 可见文案继续同步维护 `i18n/*.json`。
- 模型请求仍必须通过 `utils.ai_model_middleware`。
- Prompt 修改优先维护 `res/default_prompts.json` 和用户 prompt override 机制，不在业务代码中硬编码 prompt。
- 目标角色迁移会影响 `ProjectConfig`、项目页、输出页、编译器和角色卡生成流程，应按独立步进规划和验收。
- Proxy 设置属于运行时网络能力，需先确认作用范围：模型请求、模型列表、FFmpeg 下载或其他联网动作是否都纳入。
