# 更新日志

本文件记录 CharaPicker（拾卡姬）的重要版本变化。格式参考 Keep a Changelog，版本号与阶段遵循项目发布规范。

> 首次创建于 2026-06-01。历史版本根据 Git tag、提交记录和现有项目文档回溯整理；早期条目按发布级别归纳，不逐条列出内部临时提交。

## 未发布

### 修复

- 修复 Windows 便携版自更新因重命名整个安装根目录而触发文件占用错误的问题；更新器现在保留已验证下载包、备份将被覆盖的旧程序文件，并在原安装目录内直接覆盖新版 payload，不再移动项目、配置、日志、工具和本地模型。

## v1.0.1 - 2026-09-02

### 改进

- 完整提取现在会沿用最近的线性提取 run，校验素材来源与指纹后复用已有正式 chunk，
  只补齐缺失、损坏或来源已变化的产物；洁净提取仍会清理可再生产物并全量重建。
- 文本正式提取改为逐 chunk 原子保存检查点，后续调用中断时已成功产物可在下次完整提取中继续复用；
  集与季的聚合输入未变化时也会避免重复调用模型。

### 修复

- 修复打包版模型配置页读取测试图片、音频和视频时仍按安装根目录定位资源的问题；
  现在统一从 PyInstaller bundle 的只读资源目录加载，并在运行时健康检查和发布包门禁中验证这些素材。
- 修复 Windows 下调用 PowerShell、FFmpeg、Whisper、llama.cpp、7-Zip 等命令行工具时可能短暂弹出终端窗口的问题；
  相关运行时子进程现在统一使用无窗口启动标志。

## v1.0.0 - 2026-08-30

### 改进

- 在 `v1.0.0-rc.2` 基础上完成首个稳定基线收口；不新增顶层媒体类型，
  也不要求迁移现有项目数据。
- 默认提取 Prompt 资源升级为 v2：为合法授权的虚构素材分析补充中立、结构化索引边界，
  对敏感内容只提取高层证据事实，不要求模型规避安全策略或生成可操作细节。
- 更新检查、校验进度、运行时版本查询和关于页文案改为更直接地说明预发布范围、
  SHA-256 校验、云端调用风险、本地 API Key 保存方式和未签名 Windows 分发状态。

### 构建

- 日常开发与 GitHub Actions 构建工具统一迁移到 uv：`.python-version` 固定 Python patch，
  `uv.lock` 固定开发依赖，官方 Windows Release 继续使用独立的带 hash 精确锁。
- PyInstaller 主程序与更新器构建改用受控子进程 `PATH`，不再继承宿主机其它工具目录，
  避免外部 DLL 或 UPX 污染正式 Windows 包；新增 PATH 注入自动回归。
- 正式构建在清理旧产物和运行 PyInstaller 前检查 Release 锁、目标平台、Python、uv、
  PyInstaller 与固定环境参数，版本漂移现在会快速失败；包尾继续保留防御性复核。

### 文档

- 四语 README 对齐 1.0 首个稳定基线、主要限制、用户数据与更新保留边界、云端费用和隐私风险，
  并说明 GitHub Release、SHA-256、构建溯源与 Authenticode 各自能证明的范围。
- 第三方声明补充按需下载的 FFmpeg、whisper.cpp、llama.cpp 和 Whisper 模型许可边界；
  明确这些组件默认不随发布 ZIP 分发，未来捆绑时必须复核精确版本的许可与随附声明。
- RC 到 Release 计划记录 `v1.0.0-rc.2` 检查点、已完成分支、后续构建加固和稳定版风险接受；
  计划在稳定版 tag、GitHub Actions 与 GitHub Release 全部完成后归档。

## v1.0.0-rc.2 - 2026-08-16

### 新增

- 为项目配置、全局配置和角色卡保留最近一次有效备份；启动或打开项目遇到主文件损坏时，
  可在界面中确认后恢复备份，恢复前仍会保留损坏文件用于排查。
- 新增统一的发布就绪、依赖锁、仓库约束、Markdown 链接、发布包结构和打包态健康检查，
  并把这些检查接入 GitHub Actions 质量门禁。
- 发布包新增 `build-info.json` 与 `dependency-inventory.json`，记录源码 commit、构建目标、
  工具链、锁定依赖、包内文件摘要和可执行文件签名状态。

### 改进

- 配置、知识库、角色卡、拒绝样例、封面、导出文件、更新交接文件和二进制下载改用原子写入
  或原子发布，降低中断、崩溃或磁盘写入失败留下半成品的风险。
- 模型失败现在区分安全拒绝、能力不支持、认证、限流、网络、解析和未知错误；拒绝样例保留
  prompt 版本、请求上下文和证据来源，减少把普通故障误判为安全拒绝。
- 自动更新、FFmpeg、llama.cpp 与 Whisper 运行时下载增加来源、大小、SHA-256 和落盘后的完整性
  校验；Whisper 模型文件改为固定清单，原生音频请求增加大小上限。

### 构建

- Windows x64 发布环境固定到指定 Python patch、PyInstaller、Ruff 和带 hash 的依赖锁，
  ZIP 使用规范化时间与顺序生成，便于复核同一输入的构建结果。
- tag 发布形成“质量门禁 -> Windows 构建 -> GitHub artifact attestation -> GitHub Release”
  的串行链路；ZIP、checksum、依赖库存和最终构建清单均进入 provenance 校验范围。
- 发布前实际检查 `CharaPicker.exe` 与 `CharaPickerUpdater.exe` 的 Authenticode 状态；Release
  正文会自动附加 SHA-256 与 GitHub provenance 验证命令以及必要的信任边界说明。

### 已知限制

- 当前 Windows 二进制仍未使用 Authenticode 证书签名，系统可能显示未知发布者或
  SmartScreen 信誉提示。SHA-256 只校验下载字节，GitHub artifact attestation 只证明
  GitHub Actions 构建来源，两者都不等同于 Windows 发布者签名。

## v1.0.0-rc - 2026-07-29

### 新增

- 关于页新增手动检查更新入口与“更新测试版”偏好，可从 GitHub Releases 检查正式版或预发布版更新。
- 新增独立 `CharaPickerUpdater` 自更新辅助程序，支持下载校验、安装目录替换、用户运行时数据保留、启动确认和失败回滚。

### 构建

- 修复无后缀正式版 tag 继承当前默认开发阶段的问题；`v1.0.0` 现在固定解析为
  `version=1.0.0`、`stage=release`。
- 正式版版本标签和发布 zip 文件名不再包含 `release` 后缀；`build.bat` 根据构建元数据
  动态拼接文件名，例如 `CharaPicker-v1.0.0-windows-x64.zip`。
- 非本地构建新增 tag、应用版本与阶段一致性校验；关于页改为从统一应用元数据动态展示版本，
  避免发布包文件名与包内版本不一致。
- 默认应用版本升级为 `v1.0.0-rc`，同步更新应用元数据、Python 项目元数据和打包脚本默认值。
- 修复打包态运行根目录依赖启动工作目录的问题；frozen one-folder 环境现在以
  `CharaPicker.exe` 所在目录作为 `APP_ROOT`，自更新 relaunch cwd 同步使用安装目录。

### 文档

- 重写根 README 和多语言 README 的开头说明、当前状态与限制说明，让项目定位更像个人项目说明而不是功能清单。
- README 新增个人实验性质与纯 vibe coding 声明，并同步到繁体中文、英语和日语补充版本。
- README 不再维护精确当前版本号或手动更新时间；最新发布包和版本变化交给 GitHub Releases 与更新日志。
- 多语言 README 补齐主要依赖和构建入口，根 README 移除偏内部的 Codex 指导链接。
- 已完成的自更新 payload 识别修正和打包运行根目录修复计划移入归档，并同步更新 plans、archive 和 docs 索引。
- 文档维护规范补充 README 写作语气要求，后续更新需保留项目成熟度和风险说明。
- 记录发布前代码审查结论和打包运行根目录修复计划，明确打包态运行根目录应来自 `CharaPicker.exe` 所在目录。
- 明确 `rc.0` 与 `rc` 等价，公开发布优先使用无 `.0` 的规范预发布阶段标签。

## v0.8.0-beta - 2026-07-21

### 新增

- 正式提取引入 `FormalExtractionRunPlan` 主索引，统一视频、图片、音频和文本四种媒体类型的素材扫描、预览采样、正式分派、知识库聚合与来源追踪。
- 新增普通文本、SRT/ASS timed text、静态图片、漫画页组、音频 transcript、视频字幕关联和补充型原生音视频理解 handler；不支持的模型能力或格式会返回可解释 warning，不阻断其它素材。
- 角色卡证据新增媒体类型、内容形态、`source_trace`、`extraction_run_id` 和 `source_context.source_runs`，便于从角色卡追溯到正式提取 run 与原始素材。
- 新增结构化提取失败样例、拒绝样例和洞察媒体 metadata，便于定位模型拒绝、解析失败和多内容形态分派问题。
- 新增 ZIP、CBZ、EPUB、文本型 PDF、7z、RAR 与 CBR 的受控输入预处理，派生素材继续复用现有 text/image 提取链路。

### 变更

- transcript 统一建模为从音频或视频派生的 text 成果；原生音视频理解只补充语气、环境声、音乐和画面线索，不替代逐字 transcript。
- 输入容器原件保留在 `raw/`，派生材料按 raw 相对路径写入 `materials/derived_inputs/`，预处理 manifest 继续使用 schema v1 并记录来源、派生项、大小、fingerprint、warning 和失败项。
- 归档预处理增加路径穿越、文件名碰撞、加密内容、解压炸弹、嵌套容器和损坏文件保护；通用 ZIP/7z/RAR 不再展开视频，CBZ/CBR 只接纳漫画图片页。
- 非原始素材处理只在存在显式导入的视频时要求 FFmpeg；缺少 FFmpeg 时可以取消、忽略全部视频继续处理其它素材，或下载 FFmpeg 后自动执行。
- 项目页素材状态改用轻量完整性检查，避免频繁计算 SHA-256 阻塞 UI；复用判定和 raw 清理前的强校验仍保留，并把 raw 清理移入 Qt 工作线程。

### 修复

- 修复模型 provider 声明原生音频能力时，普通文本或视觉模型可能被错误分派到 native audio handler 的问题。
- 修复角色卡编译证据可能丢失来源 run、正式聚合可能混入非当前 run 产物的问题。
- 修复不同 raw 子目录下同名容器可能共享派生目录或 manifest 的问题，并补充派生文件大小与 SHA-256 fingerprint 校验。
- 修复归档内视频可能隐式进入 FFmpeg 流程、缺少 FFmpeg 时提前阻断非视频预处理，以及 FFmpeg 不可用时无法继续处理其它素材的问题。

### 文档

- 完成并归档提取质量与可观测性、正式提取架构重构、多内容形态覆盖和更多输入格式支持计划；更新 TODO、架构说明、提取工作流、README 与第三方许可说明。
- 新增统一多内容形态离线回归、各媒体 handler、输入预处理、GUI 状态、i18n 和真实素材验收脚本。

### 构建

- 新增 `pypdf>=6.14.2,<7` 运行时依赖，用于文本型 PDF 提取；7z/RAR/CBR 复用用户安装或项目本地的可选 7-Zip，不由应用自动下载或随默认源码依赖安装。
- 默认应用版本升级为 `v0.8.0-beta`，同步更新项目元数据、构建默认值、README、多语言 README、关于页版本文案和发布规范示例。

## v0.7.0-beta - 2026-06-05

### 新增

- 角色卡正式编译新增分层证据包：`direct_evidence_episodes`、`mention_evidence_episodes`、`causal_context_episodes` 和 `season_context`，并把结果写入 CharaPicker 扩展字段，便于追踪角色事实、提及、因果背景和季级上下文。
- 角色卡编译新增 AI 别名校验与重分类链路：当用户填写中文名或别名而知识库使用英文/罗马字候选名时，可从 `episode_content.targets` 解析已验证别名并重新匹配 direct 证据。
- 角色卡质量诊断新增 `needs_review_reasons`、`conflict_groups`、`alias_resolution` 和 `parse_diagnostics`，用于解释证据不足、低置信别名、知识库 warnings、冲突复核和 AI JSON 修复情况。

### 变更

- 没有 direct 证据的正式角色卡仍会编译失败，避免为未出场或无证据角色硬生成资料卡；`targets` 只作为别名候选和辅助信息，不单独算 direct。
- 角色卡 AI 复核 prompt 新增 `evidence_layers` 变量，beta 阶段旧的用户 prompt override 可能需要重置后才能使用新分层证据。
- `quality.warnings` 只保留用户可读 warning，结构化复核原因保存在 `extensions["charapicker"]["quality_checks"]["needs_review_reasons"]` 并由 UI 本地化展示。
- 调整模型调用、素材处理、正式提取和角色卡编译日志：INFO 记录阶段起止与结果摘要，DEBUG 记录计数、分支和 token/响应形状，WARNING/ERROR 区分可恢复降级与任务失败。
- 收紧日志中的敏感输出：模型端点统一脱敏，FFmpeg 不再打印完整命令，素材导入与知识库读取失败日志避免记录完整本地路径。

### 修复

- 修复右侧洞察流默认提示或较长事件文本可能撑出面板宽度、出现横向溢出的问题。
- 修复成功重编译后 stale reason 或失败状态残留的问题，已编译卡片重新成功后会清理旧错误与 stale warning。
- 修复结构化复核原因可能以内部 reason key 形式混入普通 warning 或导出展示的问题。

### 文档

- 更新四语种提取工作流参考文档和角色卡编译 Mermaid 流程图，补充分层证据、别名重分类、质量诊断和未出场角色失败保护。
- 更新根目录、core、gui、projects 和 docs 架构说明，明确角色卡证据分层、质量诊断扩展字段和 GUI/core 边界。
- 归档角色卡质量后续执行计划，并更新当前 TODO 与计划索引，将已完成的角色卡质量调优和日志治理移出队列，剩余工作收敛为素材覆盖、提取验证和可选增强。
- 更新运行时中间件、utils 架构说明和当前 TODO，沉淀日志等级划分、脱敏边界与关键链路观测点。
- 新增提取质量与可观测性、多素材前重构解耦和多素材种类支持三阶段计划框架，确认“P1 稳定性 -> 多素材前解耦 -> P0 多素材支持”的后续路线。

### 构建

- 默认应用版本升级为 `v0.7.0-beta`，同步更新 `pyproject.toml`、`utils/app_metadata.py`、`build.bat`、README、多语言 README、关于页版本文案和发布规范示例。

## v0.6.0-beta.1 - 2026-06-05

### 修复

- 修复 GitHub Release 发布流程未显示自动 `What's Changed`、完整 changelog 链接和 contributors 区域的问题；workflow 现在保留 `CHANGELOG.md` 版本段落作为正文开头，同时启用 GitHub 自动 release notes。

### 构建

- 默认构建阶段更新为 `beta.1`，用于重新发布 `v0.6.0-beta` 的 release notes 修正补包。

## v0.6.0-beta - 2026-06-04

### 新增

- 增加正式素材提取的三种入口模式：完整提取、洁净提取和快速提取。完整/洁净模式走高质量线性流程；快速模式支持用户确认并发数后并行提取 chunk，再用 AI 重整理集和季。
- 增加正式提取的 run 隔离与产物元数据，chunk、episode、season 产物记录 `extraction_run_id`、`extraction_stage`、`schema_version`、上下文策略和 token 用量，聚合时只消费当前 run 中合格的正式产物。
- 增加线性上下文构建能力：正式 chunk 提取会带同集已完成 chunk、同季历史 episode 和跨季长摘要，并按模型上下文窗口、128k 历史池、输出预留和安全余量控制预算。
- 增加 AI episode/season 整理链路：每集完成后由 AI 合并 `episode_content` 并生成 `episode_summary`，每季完成后由 AI 合并 `season_content` 并生成 `season_summary`。
- 增加可再生提取产物清理能力，洁净提取会清理中间文件后重新跑正式流程，并在新正式知识库写入成功后把已编译正式角色卡标记为 stale。
- 增加正式提取回归验证脚本 `scripts/validate_formal_extraction_workflow.py`，覆盖 run 隔离、模式分流、上下文预算、清理边界和角色卡 stale 标记等基础行为。

### 变更

- 正式提取进度事件现在展示更细的 run、season、episode、chunk 和 AI 整理阶段信息，项目页洞察流能区分完整、洁净和快速流程。
- 模型 preset 增加 `context_window_tokens`，用于估算上下文预算；未配置时使用保守默认值并记录策略标记。
- 角色卡编译继续只读取正式知识库；直接匹配失败时可从 `episode_content.targets` 候选中解析别名，但 AI 返回的别名必须实际存在于知识库候选中。
- 角色卡编译成功后会清理旧的错误状态，避免成功重编译后页面仍残留失败提示。

### 修复

- 修复角色卡使用中文名、别名或罗马音而知识库使用英文/候选名时可能无法匹配的问题。
- 修复角色卡编译成功后 `last_error`、warning 或失败态未及时清空导致的误导性状态展示。
- 修复正式提取失败重跑时旧产物可能混入聚合结果的风险。

### 文档

- 更新四语种提取流程参考文档，补充完整/洁净/快速提取流程图、角色卡编译流程图、跨 chunk/跨集/跨季上下文策略和当前限制。
- 归档正式素材详细提取流程与模式计划，将剩余角色卡上下文分层、验证脚本和日志治理残项迁入当前 TODO。
- 新增角色卡质量后续临时计划，覆盖 TODO P1 的角色卡编译上下文分层、冲突消解和质量评估强化。
- 更新 README、多语言 README、发布规范和产品说明中的当前版本到 `v0.6.0-beta`。

### 构建

- 默认应用版本升级为 `v0.6.0-beta`，同步更新 `pyproject.toml`、`utils/app_metadata.py` 和 `build.bat`。

## v0.5.1-beta - 2026-06-01

### 新增

- 关于页新增独立 `LICENSE` 按钮与许可证弹窗，可在应用内查看许可证说明、MPL-2.0 正文和第三方声明全文，并支持一键复制。
- 新增项目级 `CHANGELOG.md`，回溯整理 `v0.1.0-alpha.1` 到 `v0.5.0-beta` 的发布级变更，后续 tag 发布前先维护版本记录。
- 新增根目录 `LICENSE` 与 `THIRD_PARTY_NOTICES.md`，明确 CharaPicker 自有源码、第三方依赖、打包工具和运行资源的许可证边界。

### 变更

- 调整四个语系的 UI 文案语气，让启动、项目、洞察流、模型测试、提示词和关于页文本更清晰，减少过度拟人化或容易误读的表达。
- 关于页项目主页链接从通用 GitHub 首页改为 CharaPicker 仓库地址。

### 构建

- 发布包构建会复制 `README.md`、`LICENSE` 和 `THIRD_PARTY_NOTICES.md` 到发布目录。
- GitHub Actions 的 tag 发布流程显式传入 tag 构建参数，并从 `CHANGELOG.md` 抽取对应版本段落作为 GitHub Release 正文。
- Python 项目元数据补充许可证文件声明，便于打包与分发时携带许可证信息。

### 文档

- 重组 `docs/plans/`、`docs/reference/` 和 `docs/archive/` 的索引与计划归档，更新提取工作流、文档维护、roadmap 和预览摄取计划等入口说明。
- README、多语言 README、发布规范和素材声明文档补充许可证、第三方声明与公开构建合规说明。

## v0.5.0-beta - 2026-05-26

### 新增

- 增加多供应商模型接入能力，补充供应商端点元数据、供应商图标、视频输入模式和 DashScope 音频连通测试。
- 增加可选 Whisper 运行时管理与剧集音频转写链路，为视频素材提取提供音频侧输入。
- 增加多供应商与音频工作流验证覆盖。

### 修复

- 改进 Whisper 复用、模型拉取与运行时探测，避免不可访问环境导致工具状态异常。
- 修复云模型控件、模型下拉值保留、非流式测试输出和阿里云供应商行为路由等体验问题。

### 文档

- 整理多供应商集成计划和项目参考文档结构。

## v0.4.0-alpha - 2026-05-25

### 新增

- 增加运行时代理网络中间件，让模型请求和运行时网络访问可以通过统一代理配置路由。
- 增加代理设置控件，并为代理端点提供默认空值，减少首次配置阻塞。

### 文档

- 补充代理运行时网络计划与实现说明，并重组项目文档。

### 重构

- 集中共享常量，减少跨模块重复定义。

## v0.3.0-alpha - 2026-05-24

### 新增

- 增加项目内角色卡存储模型、角色卡页面工作流、角色卡导入导出与渲染能力。
- 增加从正式知识库编译角色卡的链路，并支持流式生成角色卡编译变体。
- 增加角色卡对白数量配置、编译耗时展示和应用图标。

### 修复

- 稳定角色卡编译反馈，增强流式角色卡 JSON 解析。
- 调整角色卡交互、提示和编译目标说明，让页面行为更清晰。

### 重构

- 将提取页与旧目标角色配置解耦，稳定角色卡工作台结构。

### 文档

- 更新角色卡工作流文档与 Codex 项目上下文。

## v0.2.0-alpha.1 - 2026-05-15

### 新增

- 打通正式视频素材提取 MVP：支持知识库产物隔离、视频源清单扫描、正式视频 chunk 提取和输出聚合。
- 增加季/集/chunk 知识库结构，支持 chunk 结果写入、剧集合并、剧集摘要、季内容合并和季摘要生成。
- 增加历史季背景注入开关，以及按季/集渐进编译角色状态的基础链路。
- 增加供应商拒绝 chunk 时的跳过控制。

### 修复

- 隔离预览提取产物，修复预览 token 用量展示、低 token 预算、视频输出 token 缩放和预览重新提取行为。
- 修复截断正式 chunk 响应处理、提取进行中按钮状态、提取进度与提取模式控件展示。

### 重构

- 拆分提取知识库、素材处理、FFmpeg 与界面辅助逻辑，收紧提取产物边界。

### 文档

- 补充正式视频提取计划、预览知识库行为、token 预算行为和版本规则说明。

## v0.1.0-alpha.3 - 2026-05-01

### 修复

- 修复 Windows GitHub Actions 工作流调用打包脚本的相对路径问题。

## v0.1.0-alpha.2 - 2026-05-01

### 新增

- 增加 GitHub Actions Windows 构建工作流。

### 构建

- 统一打包元数据解析，支持从构建脚本和 tag 推导版本阶段。
- 修复冻结运行时默认 prompt 路径回退。

## v0.1.0-alpha.1 - 2026-04-30

### 新增

- 建立 PyQt6 + qfluentwidgets 桌面应用骨架，包含启动页、主窗口、项目工作流、主题切换和基础页面结构。
- 增加多语言 i18n 基础、集中颜色资源、全局配置存储和日志中间件。
- 增加模型设置工作流、AI 模型中间件、prompt 自定义页面、云模型流式测试和 token 用量展示。
- 增加素材处理界面、转码/分段选项、素材导入清理流程、FFmpeg 素材处理中间件和可取消回滚。
- 增加启动预热流程、预览洞察流、可复用流式文本组件和初始 PyInstaller 文件夹式打包配置。

### 修复

- 修复空项目状态、提取操作文案、模型测试状态文案、洞察空态文案和冻结运行时路径识别。

### 文档

- 增加架构说明、多语言 README 和初始项目文档。
